.all_titles
  .all_titles__case
    全投稿小説一覧
  .all_titles__list
    - @titles.each do |title|
      .title-list-name
        = link_to title_volumes_path(title) do
          .list-name
            = title.name
